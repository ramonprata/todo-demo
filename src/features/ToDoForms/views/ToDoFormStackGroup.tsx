import React from 'react';
import { View } from 'react-native';

interface ToDoFormStackGroupProps {}

const ToDoFormStackGroup: React.FC<ToDoFormStackGroupProps> = () => {
  return <View />;
};

export default ToDoFormStackGroup;
